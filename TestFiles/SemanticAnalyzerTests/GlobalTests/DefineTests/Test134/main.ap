module main

define Set(myStruct) as StructSet

define HashSet(myStruct) as StructHashSet

define TreeSet(myStruct) as StructTreeSet

define Set(myInterface) as myInterfaceSet

define HashSet(myInterface) as myInterfaceHashSet

define TreeSet(myInterface) as myInterfaceTreeSet

struct myStruct is
    a as int
end

interface myInterface is
    fun someFun(a as int) int end
end