module main

import myStruct, myUnion, myEnum, myInterface from module secondary

define List(myStruct) as myStuctList

define Vector(myUnion) as myUnionList

define LinkedList(myInterface) as myInterfaceList

