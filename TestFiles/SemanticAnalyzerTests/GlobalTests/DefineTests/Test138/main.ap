module main

define Map(myStruct : string) as myStructMap

define Map(bob : string) as BobMap

define HashMap(myStruct : string) as myStructHashMap

define HashMap(bob : string) as BobHashMap

define Dictionary(myStruct : string) as myStructDictionary

define Dictionary(bob : string) as BobDictionary

struct myStruct is
    a as string
end

interface bob is 
    fun myMethod() end
end
