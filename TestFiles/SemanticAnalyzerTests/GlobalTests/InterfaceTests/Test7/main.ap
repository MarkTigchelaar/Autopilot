module main

import myStruct, myUnion, myEnum, myError from module others.second

interface thing is
    fun method1(a as int, b as myStruct) int end
    fun method2(c as myEnum, d as myUnion, e as myError) myError end
end