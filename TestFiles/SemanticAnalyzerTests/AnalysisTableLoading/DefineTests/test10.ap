module four


define List(somestuff) as ListOfStuff

define Queue(things) as ThingsQueue

define FifoQueue(things) as ThingsFifo

define PriorityQueue(things) as PriorityThings

define Deque(stuff) as StuffDeque

define TreeSet(int) as IntTree

define HashSet(float) as FloatSet

define Set(All) as AllSet
