class ForLoopAnalyzer
    def initialize(main_analyzer)
        @main_analyzer = main_analyzer
    end

    def analyze_node_locally(ast_node)
        return
        # most complicated loop
        # check names of optional types, name collisions to variables, and loop name
        # checkrange type things etc.
        # register name, and register with analyzer as being in a loop
    end
end
