acyclic pub fun second(a as int = 0) someType do
    let b = 1 + 2
    if b > a do
      return SomeType(a, 1 + 1)
    elif a > b do
      return SomeType(b, 1 + 1)
    else
      print("test") // printing "test" stops everything from returning a json object??
    end
end